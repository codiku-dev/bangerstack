# PORT (éviter `--port` en CLI si tu vois des soucis Turbopack). Next doit être joignable sur le LAN : dev:web = -H 0.0.0.0 + --webpack (doc Capacitor live reload).
PORT=3001

NEXT_PUBLIC_API_BASE_URL=http://localhost:3090
NEXT_PUBLIC_GOOGLE_AUTH_CALLBACK_URL=http://localhost:3000
